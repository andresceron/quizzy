export const LOCAL = 'local';

export enum LOG_LEVELS {
  ERROR = 'error',
  WARNING = 'warn',
  INFO = 'info',
  HTTP = 'http',
  VERBOSE = 'verbose',
  DEBUG = 'debug'
}

export enum NODE_ENV {
  DEV = 'dev',
  TEST = 'test',
  PROD = 'production'
}
